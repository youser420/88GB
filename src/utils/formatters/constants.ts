/** Locale used by all formatters. Centralised for easy future i18n. */
export const DEFAULT_LOCALE = 'en-US';

/** Placeholder rendered when a value is missing or not finite. */
export const EMPTY_VALUE = '—';
